int add(int f, int g)
{ 
	return (f + g);
}

int sub(int f, int g)
{
	return (f - g);
}

int mul(int f, int g)
{
	return (f * g);
}

int div(int f, int g)
{
        return (f / g);
}

int mod(int f, int g)
{
        return (f % g);
}
